#pragma once

// Declare fn's so can use elsewhere
// int add(int i, int j);
// void pybind11_testing();
// int bts_test(double alpha, bool use_python_env);
// int bts_test_cpp_env(double alpha);
// int bts_test_py_env(double alpha);
